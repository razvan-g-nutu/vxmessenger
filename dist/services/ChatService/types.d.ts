import { Abilities, ChargeInfo, Chat as CoreChat, ChatConfig, ExitCode, Limits, Query, SourceSet, TargetSet, User, UserEvent, UserEventType } from 'vchat-core';
export { Abilities, ChatConfig, ChargeInfo, ExitCode, Limits, Query, CoreChat, SourceSet, TargetSet, User, UserEvent, UserEventType };
export declare enum GET_CHAT_ERROR {
    BAD_CHAT_CONFIG = "BAD_CHAT_CONFIG"
}
