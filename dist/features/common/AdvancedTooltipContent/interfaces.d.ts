/**
 * Corrections that should be applied to a normal centered tooltip
 * (Centered = having the arrow placed on the middle of the tooltip edge)
 * Could be positive or negative values
 */
export interface ICorrections {
    horizontal: number;
    vertical: number;
}
