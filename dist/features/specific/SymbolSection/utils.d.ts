import { IContainerProps as IProps } from "./types";
export declare const emoticonClick: (props: IProps) => (emoticonShortcut: string) => () => void;
export declare const giftClick: (props: IProps) => (giftShortcut: string) => () => void;
