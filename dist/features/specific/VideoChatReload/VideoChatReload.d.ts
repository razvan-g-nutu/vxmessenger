import { ITranslation } from '../../../redux/store';
interface IProps {
    translation: ITranslation;
    onClick: () => any;
}
declare const VideoChatReload: (props: IProps) => JSX.Element;
export default VideoChatReload;
